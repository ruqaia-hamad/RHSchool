
- convert the json into java objects.
gender
name
location
email
login
registered
dob
phone
cell
id
picture
nat

- Fetch data from the  random user API .Call the API to generate users and you will recive users Randomly ,get Rsults as String .
String url = "https://randomuser.me/api/";

HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder().uri(URI.create(url)).build();

HttpResponse<String> response = client.send(request, HttpResponse.BodyHandlers.ofString());

-object chaining to Data class that contain  object  of result and info class 
	Data data = new Gson().fromJson(response.body(), Data.class);

Multiple users:


If user input 0 result will be show default format.
or accourding to user Input number of user will be shown.

-consume the API using parametrs to get specific Result:

password :

{"https://randomuser.me/api/?password="+password"}

to show secifed password for user , we consume the APi with parametr password .for example :
IF uesr input upper only uppercase passwords will be shown.

Specifying a gender :

specify whether you would like to have only male or only female users generated by adding the gender parameter .
String url = "https://randomuser.me/api/" + "?gender=" + gender".




API Errors:
class to handle error if error accor for Api . return a simple JSON object with an error.
	ErrorHandler errorhandler = gson.fromJson(responseB, ErrorHandler.class);
	System.out.println(errorhandler.getError());
	
	
seeding:
	String url = "https://randomuser.me/api/" +  "&seed=" + seed ".
 allow you to always generate the same set of users.
 
 Including/Excluding fields:
 including--- for showing data
 excluding --- not showing in data 
 and i used if else stamemnt 
 
	
	
	
	
	

